"""Missions: a personal TODO/Note kanban whose defining trait is session-hold.

Separate agent sessions read missions and see which session holds each one, via
the missions MCP (flightdeck.mcp_server) which shares this same SQLite store.
See MISSIONS_MVP_CONTRACT.md at the repo root.
"""
